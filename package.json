{
  "name": "@iyen/openclaw-extensions",
  "version": "0.1.0",
  "private": true,
  "description": "IYENTeam's OpenClaw extension monorepo. Each package is a self-contained OpenClaw plugin (context engines, watchdogs, runtime add-ons) registered via the standard `openclaw.extensions` entry.",
  "type": "module",
  "scripts": {
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.5.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IYENTeam/openclaw-extensions.git"
  }
}
